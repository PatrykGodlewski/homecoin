# Scripts

| Path | Purpose |
|------|---------|
| `ci/` | CI and E2E — `smoke_test.sh`, `install-playwright.sh` |
| `dev/` | Local dev data — `seed.sql`, `seed_db.sh` (`make seed`) |

Azure deployment scripts live in [`infra/scripts/`](../infra/scripts/).
